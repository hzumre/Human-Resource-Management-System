@using IK_Project.UI.Areas.Admin.Models.ViewModels.RoleVMs;

@{
    ViewData["Title"] = "GetAllRoles";
}

@model List<RoleListVM>

<div class="row">
@*    <div class="col-lg-12">
        <a asp-action="Create" class="btn btn-primary">Create New Role</a>
    </div>*@
</div>
<br />
<br />
<div class="row">
    <div class="col-lg-12">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Role</th>
                    @*<th>Edit</th>
                    <th>Delete</th>*@
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Name</td>
                        @*<td>
                            <a class="btn btn-secondary" asp-controller="Role" asp-action="Edit" asp-area="Admin" asp-route-id="@item.Id">Edit</a>
                        </td>
                        <td>
                            <a class="btn btn-danger" asp-controller="Role" asp-action="Delete" asp-area="Admin" asp-route-id="@item.Id">Delete</a>
                        </td>*@
                    </tr>
                }
            </tbody>

        </table>

    </div>
</div>

